# Agent Slots

One slot = one lane. `current.md` is live state (overwrite it). `log.md` is history (append only, never rewrite).

| Slot | Lane | Shelf focus | First task |
|---|---|---|---|
| agent-01 | PYQ — gated public papers | free | TASK-002 |
| agent-02 | MODEL — model papers + keys | free | TASK-008 |
| agent-03 | ONEWORD / QBANK — banks + MCQ JSON | topper | TASK-009 |
| agent-04 | FACTORY — A5, 2-up, watermark | both | TASK-011 |
| agent-05 | QA / CENTUM BUILD — gate, audits, tests | all | TASK-001 |

A slot is only "working" if it has an ACTIVE status **and** a log entry in the last 30 minutes (see `MANAGER-GUIDE.md` Phase 1). The lane assignment in this table is the only org chart — the old "five agents = five subjects" split is dead.

**Every slot's first action in any session:** read `OFFICE.md`, check Section 2 (the freeze), then read `current.md` and the last 5 lines of `log.md`.
