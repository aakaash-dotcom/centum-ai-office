# agent-02 — MODEL Lane

STATUS: STAGED (awaiting owner GO — freeze in force)
ROLE: MODEL — model question papers + answer keys (recover existing before creating new)
TASK: TASK-008 — Recover the existing 10th model papers + keys (queued)
PROGRESS: 0%
FILES PRODUCED: 0
BLOCKER: none
NEXT STEP: on GO — locate the old 10th Maths/Science model papers + keys on Drive, gate them, publish into Models/
STOP CONDITION: the recovered models + keys open correctly and sit in `StudyHub/TN/10th/<Subject>/Models/`, with a written list of what was found and what is missing

---

## Your lane in one line
Free-shelf model papers and their answer keys. **Recovery first, creation second.** The team already made 10th Maths/Science models + keys; they were deleted from the local workspace during the 23 Sep cleanup and may still be on Drive. Find them before you write a single new question.

## Where to look
- `Question Papers/10th/**` (old vault, year folders, `Unknown/`, `WITHKEY/`, `2UP-A4/`, `Print-Ready/`)
- `Study Materials/**` (guides/SLM inventory was never finished)
- Anything named like `*model*`, `*key*`, `*answer*`, `*_A5.pdf` under 10th
- Drive search is unreliable here — walk one folder path at a time with `list`.

## The rule that matters most
**Never regenerate what already exists.** Recreating a model paper is a wasted session and produces a second, conflicting version. Search, gate, recover, publish. Only then fill gaps.

## Your standards
- Board pattern exact (marks split, duration, instructions header). Brand line on the header.
- Answer key is a separate file with marking points in the order a teacher ticks them — never a bare answer list.
- No claim of official endorsement, ever.
- `training/quality-checklist.md` §5 is your checklist.

## Techniques
Same Drive bridge rules as every lane (`OFFICE.md` §5.1). Originals never watermarked; the FACTORY lane produces the printable copy. Log every recovered file in `ledgers/publish_log.csv` with its **old path** and its new path.
